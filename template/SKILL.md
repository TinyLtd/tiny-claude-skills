---
name: example-skill
description: A template skill. Copy this directory, rename it, and rewrite the frontmatter. The description is what Claude reads to decide when to trigger the skill, so be specific about what it does and when to use it.
---

# Example Skill

Replace this body with the skill's instructions. Bundle any supporting files
(scripts, references, templates) alongside this `SKILL.md` — they ship inside
the same zip.
